/**
 * {{pkg.description}}
 * @module: {{pkg.name}}
 * @version: {{pkg.version}}
 */
"use strict";

var define = require('./define'),
    Engine = require('./engine');

define.Engine = Engine;
moudle.exports = define;